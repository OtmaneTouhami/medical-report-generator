import { Fingerprint } from 'lucide-react';

// Re-export with a name that won't trigger content blockers
export const AuthIcon = Fingerprint;

// Export other icons as needed
export * from 'lucide-react'; 